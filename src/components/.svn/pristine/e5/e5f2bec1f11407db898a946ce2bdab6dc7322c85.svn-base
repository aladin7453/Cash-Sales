"use client";
import { FaPlus } from 'react-icons/fa6';
import { Pagination } from "@/components/data-table/Pagination";
import Refresh from "@/components/data-table/Refresh";
import { Button } from "@/components/ui/button";
import type { Table } from "@tanstack/react-table";

type Props<TData> = {
  table: Table<TData>;
  onAdd?: () => void;
  showAddButton?: boolean; // New prop to control Add button visibility
};

export default function DropdownTableToolbar<TData>({ table, onAdd, showAddButton = false }: Props<TData>) {
  return (
    <div className="min-w-[450px] max-w-xl bg-white flex justify-between items-center px-1.5">
      {showAddButton && (
        <div className="flex flex-col items-center">
          <Button
            type="button"
            className="size-8"
            variant="ghost"
            size="icon"
            onClick={onAdd}
          >
            <FaPlus className="size-5.5" />
          </Button>
          <span className="text-[11px]/none font-medium text-black">Add</span>
        </div>
      )}
      <Pagination table={table} formView={true} showGoButton={false}/>
      <Refresh isMobile={true} />
    </div>
  );
}