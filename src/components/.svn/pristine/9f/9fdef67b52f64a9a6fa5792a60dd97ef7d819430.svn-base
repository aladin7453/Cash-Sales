import { Progress } from "./ui/progress";

type Params = {
  progressValue?: number;
};

export default function LoadingUI({ progressValue = 75 }: Params) {
  return (
    <div className="fixed inset-0 z-50 flex size-full items-center justify-center bg-black/15">
      <Progress value={progressValue} className="w-1/4 bg-erp-gray-1" />
    </div>
  );
}
