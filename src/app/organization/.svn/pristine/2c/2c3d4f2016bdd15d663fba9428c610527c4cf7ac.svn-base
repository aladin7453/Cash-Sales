"use client";

import React from 'react';
import { Dialog, DialogContent } from '@/components/ui/dialog';
import { Button } from '@/components/ui/button';

const ConfirmationDialog = ({ open, setOpen, onConfirm }) => {
  return (
    <Dialog open={open} onOpenChange={setOpen}>
      <DialogContent>
        <p>Are you sure you want to delete the selected row(s)?</p>
        <div className="flex justify-end gap-4 mt-4">
          <Button onClick={onConfirm}>Yes</Button>
          {/* Ensure the "No" button is red by using bg-red-500 */}
          <Button onClick={() => { setOpen(false); }} className="bg-red-500 text-white">No</Button>
        </div>
      </DialogContent>
    </Dialog>
  );
};

export default ConfirmationDialog;
