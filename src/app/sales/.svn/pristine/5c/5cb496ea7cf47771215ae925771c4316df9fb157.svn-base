import type { Metadata } from "next";

export const metadata: Metadata = {
  //title: "Sales Credit Note",
};

export default function SalesCreditNotePageLayout({ children }: { children: React.ReactNode }) {
  return children;
}
