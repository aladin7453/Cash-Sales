import type { Metadata } from "next";

export const metadata: Metadata = {
  //title: "Tag",
};

export default function AgentPageLayout({ children }: { children: React.ReactNode }) {
  return children;
}
