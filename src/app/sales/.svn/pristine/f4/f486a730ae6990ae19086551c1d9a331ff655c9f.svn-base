import type { Metadata } from "next";

export const metadata: Metadata = {
  //title: "Cash Sales",
};

export default function CashSalesPageLayout({ children }: { children: React.ReactNode }) {
  return children;
}
