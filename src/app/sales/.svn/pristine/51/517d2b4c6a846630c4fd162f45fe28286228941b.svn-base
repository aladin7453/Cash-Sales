import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Customer Document Listing",
};

export default function CustomerDocumentListingPageLayout({ children }: { children: React.ReactNode }) {
  return children;
}