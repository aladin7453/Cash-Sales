import type { Metadata } from "next";
export const metadata: Metadata = {

  title: "Sales Price History Report",
};
export default function SalesPriceHistoryReportPageLayout({ children }: { children: React.ReactNode }) {
  return children;
}
