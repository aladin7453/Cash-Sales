"use client";

import React from "react";

import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { useWatch } from "react-hook-form";
import {
  Form,
  FormControl,
  FormField,
  FormItem,
  FormLabel,
  FormMessage,
} from "@/components/ui/form";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { Tooltip, TooltipContent, TooltipProvider, TooltipTrigger } from "@/components/ui/tooltip";

type Params = {
  form: any;
  isMobile?: boolean;
};

export function ShippingRecipientDetailsForm({ form, isMobile }: Params) {

  if (isMobile) {
    return (
      <>
        <div className="flex flex-col gap-y-4 px-4 py-2.5 bg-gray-100/10">
          <h1 className="mt-3 font-bold uppercase text-black">Recipient Details</h1>

          {/* First Row */}
          <div className="grid grid-cols-2 gap-x-3 items-center">
            {/* Name */}
            <FormField
              control={form.control}
              name="recipient"
              render={({ field }) => (
                <FormItem className="flex flex-col">
                  <FormLabel className="text-[11px] font-semibold tracking-wider text-black">Recipient Name</FormLabel>
                  <FormControl>
                    <Input
                      className="h-10 bg-white border border-gray-200 px-2 text-[11px] rounded-md outline-none transition-all duration-300 ease-in-out shadow-sm focus-visible:ring-blue-300"
                      {...field}
                      placeholder="Recipient Name"
                    />
                  </FormControl>
                  <FormMessage />
                </FormItem>
              )}
            />

            {/* TIN */}
            <FormField
              control={form.control}
              name="recipientTIN"
              render={({ field }) => (
                <FormItem className="flex flex-col">
                  <FormLabel className="text-[11px] font-semibold tracking-wider text-black">Recipient TIN</FormLabel>
                  <FormControl>
                    <Input
                      className="h-10 bg-white border border-gray-200 px-2 text-[11px] rounded-md outline-none transition-all duration-300 ease-in-out shadow-sm focus-visible:ring-blue-300"
                      {...field}
                      moduleName="salesCreditNote"
                      fieldName="recipientTIN"
                      columnName="Recipient TIN"
                      placeholder="Recipient TIN"
                    />
                  </FormControl>
                  <FormMessage />
                </FormItem>
              )}
            />
          </div>

          {/* Second Row */}
          <div className="grid grid-cols-2 gap-x-3 items-center">
            {/* SST */}
            <FormField
              control={form.control}
              name="recipientSSTNo"
              render={({ field }) => (
                <FormItem className="flex flex-col">
                  <FormLabel className="text-[11px] font-semibold tracking-wider text-black">Recipient SST No</FormLabel>
                  <FormControl>
                    <Input
                      className="h-10 bg-white border border-gray-200 px-2 text-[11px] rounded-md outline-none transition-all duration-300 ease-in-out shadow-sm focus-visible:ring-blue-300"
                      {...field}
                      placeholder="Recipient SST No"
                    />
                  </FormControl>
                  <FormMessage />
                </FormItem>
              )}
            />

            {/* BRN */}
            <FormField
              control={form.control}
              name="recipientBRN"
              render={({ field }) => (
                <FormItem className="flex flex-col">
                  <FormLabel className="text-[11px] font-semibold tracking-wider text-black">Recipient BRN</FormLabel>
                  <FormControl>
                    <Input
                      className="h-10 bg-white border border-gray-200 px-2 text-[11px] rounded-md outline-none transition-all duration-300 ease-in-out shadow-sm focus-visible:ring-blue-300"
                      {...field}
                      placeholder="Recipient BRN"
                    />
                  </FormControl>
                  <FormMessage />
                </FormItem>
              )}
            />
          </div>

          {/* Third Row */}
          <div className="grid grid-cols-1 items-center">
            {/* TTx No */}
            <FormField
              control={form.control}
              name="recipientTTXNo"
              render={({ field }) => (
                <FormItem className="flex flex-col">
                  <FormLabel className="text-[11px] font-semibold tracking-wider text-black">Recipient TTx No</FormLabel>
                  <FormControl>
                    <Input
                      className="h-10 bg-white border border-gray-200 px-2 text-[11px] rounded-md outline-none transition-all duration-300 ease-in-out shadow-sm focus-visible:ring-blue-300"
                      {...field}
                      placeholder="Recipient TTx No"
                    />
                  </FormControl>
                  <FormMessage />
                </FormItem>
              )}
            />
          </div>

          {/* Recipient Address */}
          <FormField
            control={form.control}
            name="recipientAddress"
            render={({ field }) => (
              <FormItem className="flex flex-col">
                <FormLabel className="text-[11px] font-semibold tracking-wider text-black">Recipient Address</FormLabel>
                <FormControl>
                  <Textarea
                    className="h-10 bg-white border border-gray-200 px-2 text-[11px] rounded-md outline-none transition-all duration-300 ease-in-out shadow-sm focus-visible:ring-blue-300"
                    {...field}
                    moduleName="salesCreditNote"
                    fieldName="recipientAddress"
                    columnName="Recipient Address"
                    placeholder="Recipient Address"
                  />
                </FormControl>
                <FormMessage />
              </FormItem>
            )}
          />
        </div>
      </>
    )
  }

  return (
    <Form {...form}>
      <form>
        <div className="flex flex-col gap-y-2 pt-1.5">
          {/* First Row */}
          <div className="grid grid-cols-3 items-center gap-x-2">
            {/* Name */}
            <FormField
              control={form.control}
              name="recipient"
              render={({ field }) => (
                <FormItem>
                  <FormControl>
                    <TooltipProvider delayDuration={1000}>
                      <Tooltip>
                        <TooltipTrigger asChild>
                          <Input
                            className="h-7 rounded-md px-2 text-[11px] outline-none transition-all duration-300 ease-in-out"
                            {...field}
                            placeholder="Recipient Name"
                          />
                        </TooltipTrigger>
                        <TooltipContent side="top" align="center">
                          Recipient Name
                        </TooltipContent>
                      </Tooltip>
                    </TooltipProvider>
                  </FormControl>
                  <FormMessage />
                </FormItem>
              )}
            />

            {/* TIN */}
            <FormField
              control={form.control}
              name="recipientTIN"
              render={({ field }) => (
                <FormItem>
                  <FormControl>
                    <TooltipProvider delayDuration={1000}>
                      <Tooltip>
                        <TooltipTrigger asChild>
                          <Input
                            className="h-7 rounded-md px-2 text-[11px] outline-none transition-all duration-300 ease-in-out"
                            {...field}
                            moduleName="salesCreditNote"
                            fieldName="recipientTIN"
                            columnName="Recipient TIN"
                            placeholder="Recipient TIN"
                          />
                        </TooltipTrigger>
                        <TooltipContent side="top" align="center">
                          Recipient TIN
                        </TooltipContent>
                      </Tooltip>
                    </TooltipProvider>
                  </FormControl>
                  <FormMessage />
                </FormItem>
              )}
            />

            {/* SST */}
            <FormField
              control={form.control}
              name="recipientSSTNo"
              render={({ field }) => (
                <FormItem>
                  <FormControl>
                    <TooltipProvider delayDuration={1000}>
                      <Tooltip>
                        <TooltipTrigger asChild>
                          <Input
                            className="h-7 rounded-md px-2 text-[11px] outline-none transition-all duration-300 ease-in-out"
                            {...field}
                            placeholder="Recipient SST No"
                          />
                        </TooltipTrigger>
                        <TooltipContent side="top" align="center">
                          Recipient SST No
                        </TooltipContent>
                      </Tooltip>
                    </TooltipProvider>
                  </FormControl>
                  <FormMessage />
                </FormItem>
              )}
            />
          </div>

          {/* Second Row */}
          <div className="grid grid-cols-3 items-center gap-x-2">
            {/* BRN */}
            <FormField
              control={form.control}
              name="recipientBRN"
              render={({ field }) => (
                <FormItem>
                  <FormControl>
                    <TooltipProvider delayDuration={1000}>
                      <Tooltip>
                        <TooltipTrigger asChild>
                          <Input
                            className="h-7 rounded-md px-2 text-[11px] outline-none transition-all duration-300 ease-in-out"
                            {...field}
                            placeholder="Recipient BRN"
                          />
                        </TooltipTrigger>
                        <TooltipContent side="top" align="center">
                          Recipient BRN
                        </TooltipContent>
                      </Tooltip>
                    </TooltipProvider>
                  </FormControl>
                  <FormMessage />
                </FormItem>
              )}
            />

            {/* TTx No */}
            <FormField
              control={form.control}
              name="recipientTTXNo"
              render={({ field }) => (
                <FormItem>
                  <FormControl>
                    <TooltipProvider delayDuration={1000}>
                      <Tooltip>
                        <TooltipTrigger asChild>
                          <Input
                            className="h-7 rounded-md px-2 text-[11px] outline-none transition-all duration-300 ease-in-out"
                            {...field}
                            placeholder="Recipient TTx No"
                          />
                        </TooltipTrigger>
                        <TooltipContent side="top" align="center">
                          Recipient TTx No
                        </TooltipContent>
                      </Tooltip>
                    </TooltipProvider>
                  </FormControl>
                  <FormMessage />
                </FormItem>
              )}
            />
          </div>

          {/* Third Row */}
          <div className="flex-1">
            {/* Receipt Address */}
            <FormField
              control={form.control}
              name="recipientAddress"
              render={({ field }) => (
                <FormItem>
                  <FormControl>
                    <Textarea
                      className="h-[127px] resize-none text-[11px] outline-none transition-all duration-300 ease-in-out"
                      {...field}
                      moduleName="salesCreditNote"
                      fieldName="recipientAddress"
                      columnName="Recipient Address"
                      placeholder="Recipient Address"
                    />
                  </FormControl>
                  <FormMessage />
                </FormItem>
              )}
            />
          </div>
        </div>
      </form>
    </Form>
  );
}
