import type { Metadata } from "next";

export const metadata: Metadata = {
  //title: "Customer Payment",
};

export default function CustomerPaymentPageLayout({ children }: { children: React.ReactNode }) {
  return children;
}
