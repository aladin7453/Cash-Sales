"use client";

import React, { useState } from "react";
import { FaMinus, FaPlus } from "react-icons/fa6";
import type { Table } from "@tanstack/react-table";

import { useSearchParams ,useParams} from "next/navigation";

import ColumnVisibilityToggle from "@/components/data-table/ColumnVisibilityToggle";
import Export from "@/components/data-table/Export";
import { Pagination } from "@/components/data-table/Pagination";
import Refresh from "@/components/data-table/Refresh";
// import Validity from "@/components/data-table/Validity";
import { Button } from "@/components/ui/button";
import { useToast } from "@/components/ui/use-toast";
import { 
  Tooltip, 
  TooltipTrigger, 
  TooltipContent, 
  TooltipProvider 
} from "@/components/ui/tooltip";

import { getAuthHeaders, ORIGIN } from "@/lib/constants";

import ConfirmationDialog from "./ConfirmationDialog";

// Mobile View (Imports)
import MobileFormTableActionGroupStart from "@/components/form/MobileFormTableActionGroupStart";
import MobileFormTableActionGroupEnd from "@/components/form/MobileFormTableActionGroupEnd";
import { DropdownMenuItem } from "@/components/ui/dropdown-menu";

type Props<TData> = {
  table: Table<TData>;
  onAdd?: () => void;
  setTempRowAddressDetailsData: (data: any, overwrite: boolean) => void;
  revalidateAddressDetails: () => void;
  tempRowAddressDetails: any;
};

export default function AddressDetailsDataTableToolbar<TData>({
  table,
  onAdd,
  tempRowAddressDetails,
  setTempRowAddressDetailsData,
  revalidateAddressDetails,
}: Props<TData>) {
  const searchParams = useSearchParams();
  const id = searchParams.get("id");
  const { toast } = useToast();
  const headers = getAuthHeaders();
  const { slug } = useParams()
  const [showConfirmationDialog, setShowConfirmationDialog] = useState(false);
  const [selectedRow, setSelectedRow] = useState("");

  const handleDeleteConfirmation = async () => {
    // Perform deletion logic here
    var form_data = new FormData();

    const selectedRows = table.getSelectedRowModel().rows;

    selectedRows.forEach((row, index) => {
      form_data.append(`UUIDs[]`, row["original"]["UUID"]);
    });

    if (id && slug=="edit") {
      try {
        const response = await fetch(
          `${ORIGIN}/customer/api/customer/remove-customer-has-delivery-location-data`,
          {
            method: "POST",
            headers,
            body: form_data,
          },
        );

        if (!response.ok) {
          throw new Error("Failed to create new stock data");
        }

        const responseData = await response.json();

        if (responseData) {
          toast({
            title: responseData.message,
            duration: 2000,
          });
        }
      } catch (error) {
        console.error("Error creating new delivery location data:", error);
      }

      revalidateAddressDetails();
      setShowConfirmationDialog(false);
    } else {
      const deletedIndex = selectedRows.map((item) => {
        return item.index;
      });

      const remainRows = tempRowAddressDetails.filter(
        (item, index) => !deletedIndex.includes(index),
      );

      setTempRowAddressDetailsData(remainRows, true);
      setShowConfirmationDialog(false);
    }
  };

  return (
    <>
      {/* Card Section (blue container) */}
      <div className="flex items-center justify-between rounded-t-md p-1.5">
        {/* Mobile View (Start Action Buttons) */}
        <MobileFormTableActionGroupStart
          table={table}
          config={{
            core: {
              add: { 
                handler: () => onAdd?.(),  
              },
              remove: { 
                handler: () => setShowConfirmationDialog(true) 
              },
            },
          }}
        >
          {/* Additional Action other than Add and Remove */}
        </MobileFormTableActionGroupStart>

        {/* Buttons for larger screens */}
        {/* Web View (Start Action Buttons) */}
        <div className="hidden md:flex items-center gap-x-1 pl-2 text-erp-blue-14">
          {/* Add & Remove buttons moved to the left */}
          <div className="flex items-center gap-x-1 text-erp-blue-14">
            <div className="flex flex-col items-center">
              <TooltipProvider delayDuration={0}>
                <Tooltip>
                  <TooltipTrigger asChild>
                    <Button
                      className="size-8"
                      variant="ghost"
                      size="icon"
                      onClick={onAdd}
                    >
                      <FaPlus className="size-5.5" />
                    </Button>
                  </TooltipTrigger>
                  <TooltipContent>
                    Address Details
                  </TooltipContent>
                </Tooltip>
              </TooltipProvider>
              <span className="text-[11px]/none font-medium text-black">Add</span>
            </div>

            <div className="flex flex-col items-center">
              <Button
                className="size-8"
                variant="ghost"
                size="icon"
                onClick={() => setShowConfirmationDialog(true)}
              >
                <FaMinus className="size-5.5" />
              </Button>
              
              <span className="text-[11px]/none font-medium text-black">Remove</span>
            </div>
          </div>
        </div>

        {/* Mobile View (Hide Pagination) */}
        <div className="hidden md:block">
          <Pagination table={table} />
        </div>
    
        {/* Mobile View (End Action Buttons) */}
        <MobileFormTableActionGroupEnd
          table={table}
          config={{
            search: {
              column: "deliveryLocation",
              placeholder: "Search Designated Location",
            },
            sort: {
              column: "deliveryLocation"
            }
          }}
        >
          <Export table={table} />

          {/* Mobile View (Hide Refresh and Column Visibility Toggle) */}
          <div className="hidden md:flex items-center gap-x-3">
            <Refresh refreshTable={revalidateAddressDetails} />
            <ColumnVisibilityToggle table={table} />
          </div>
        </MobileFormTableActionGroupEnd>    
      </div>
  
      <ConfirmationDialog
        open={showConfirmationDialog}
        setOpen={setShowConfirmationDialog}
        onConfirm={handleDeleteConfirmation}
      />
    </>
  );
}
