"use client";

import type { ColumnDef } from "@tanstack/react-table";

import DescriptionCellTooltip from "@/components/data-table/DescriptionCellTooltip";
import {autoWordSize} from "@/lib/constants";
import filterWithStringConversion from "@/lib/utils/data-table/filterWithStringConversion";

export type Quotation = {
  UUID: string;
  docNo: string;
  docDate: string;
  salesAgent: string;
  servicingAgent: string;
  collectionAgent: string;
  salesAgentName: string;
  servicingAgentName: string;
  collectionAgentName: string;
  creditTerm: string;
  customerCode: string;
  customerName: string;
  BRN: string;
  TIN: string;
  SSTNo: string;
  address: string;
  attention: string;
  phoneNo: string;
  email: string;
  totalRecord: string;
  currency: string;
  currencyRate: string;
  project: string;
  docValidity: string;
  shipper: string;
  taxExNo: string;
  remark1: string;
  remark2: string;
  remark3: string;
  remark4: string;
  deliveryTerm: string;
  deliveryAddress: string;
  note: string;
  attachment: string;
  attachmentLink?: {
    name: string;
    link: string;
  }[];
  attachmentFiles?: any[];
  description: string;
  approvalStatus: string;
  rejectReason: string;
  createdAt: string;
  createdBy: string;
  updatedAt: string;
  updatedBy: string;
  valid: string;
  _version: string;
  _actionUUID: string;
  currencyCode: string;
  attentionName: string;
  creditTermCode: string;
  customerCodeCode: string;
  projectCode: string;
  shipperCode: string;
  docDateFormat: string;
  createdAtFormat: string;
  updatedAtFormat: string;
  CreatedByUsername: string;
  UpdatedByUsername: string;
  cashSalesTransferred: string[];
  salesOrderTransferred: string[];
  jobOrderTransferred: string[];
  deliveryOrderTransferred: string[];
  salesInvoiceTransferred: string[];
  salesDebitNoteNo: string;
  totalQuantity: string;
  totalSubtotal: string;
  totalTaxAmount: string;
  totalSubtotalTax: string;
  localNetTotal: string;
  netTotal: string;
  preferenceData?: any;
  customerPaymentNo:string;
};

// Preference (Format Number Function)
const formatNumber = (value: string | number, decimalPlaces: number = 2): string => {
  if (value === null || value === undefined || value === '') {
    return (0).toFixed(decimalPlaces);
  }
  
  const numValue = typeof value === 'string' ? parseFloat(value) : value;
  
  if (isNaN(numValue)) {
    return (0).toFixed(decimalPlaces);
  }
  
  return numValue.toFixed(decimalPlaces);
};

const rawColumns: ColumnDef<Quotation>[] = [
  {
    id: "docNo",
    accessorKey: "docNo",
    header: "Doc. No.",
    filterFn: "includesString",
  },
  {
    id: "docDateFormat",
    accessorKey: "docDateFormat",
    header: "Doc. Date",
    filterFn: "includesString",
  },
  // {
  //   id: "salesDebitNoteNo",
  //   accessorKey: "salesDebitNoteNo",
  //   header: "DN No.",
  //   filterFn: "includesString",
  // },
  {
    id: "salesAgentName",
    accessorKey: "salesAgentName",
    header: "Sales Agent",
    filterFn: "includesString",
  },
  {
    id: "servicingAgentName",
    accessorKey: "servicingAgentName",
    header: "Servicing Agent",
    filterFn: "includesString",
  },
  {
    id: "collectionAgentName",
    accessorKey: "collectionAgentName",
    header: "Collection Agent",
    filterFn: "includesString",
  },
  {
    id: "customerCodeCode",
    accessorKey: "customerCodeCode",
    header: "Customer Code",
    filterFn: "includesString",
  },
  {
    id: "customerName",
    accessorKey: "customerName",
    header: "Customer Name",
    filterFn: "includesString",
  },

  {
    id: "BRN",
    accessorKey: "BRN",
    header: "BRN",
    filterFn: "includesString",
  },
  {
    id: "TIN",
    accessorKey: "TIN",
    header: "TIN",
    filterFn: "includesString",
  },
  {
    id: "SSTNo",
    accessorKey: "SSTNo",
    header: "SST No.",
    filterFn: "includesString",
  },
  {
    id: "address",
    accessorKey: "address",
    header: "Address",
    filterFn: "includesString",
  },
  {
    id: "attentionName",
    accessorKey: "attentionName",
    header: "Attention",
    filterFn: "includesString",
  },
  {
    id: "phoneNo",
    accessorKey: "phoneNo",
    header: "Phone No.",
    filterFn: "includesString",
  },
  {
    id: "email",
    accessorKey: "email",
    header: "Email",
    filterFn: "includesString",
  },
  {
    id: "totalRecord",
    accessorKey: "totalRecord",
    header: "Total Record",
    cell: ({ row }) => {
      return <p className="text-right">{row.original.totalRecord || "0"}</p>;
    },
    filterFn: "includesString",
  },
  {
    id: "totalQuantity",
    accessorKey: "totalQuantity",
    header: "Total Quantity",
    cell: ({ row }) => {
      return <p className="text-right">{row.original.totalQuantity || "0"}</p>;
    },
    filterFn: "includesString",
  },
  {
    id: "totalSubtotal",
    accessorKey: "totalSubtotal",
    header: "Total Subtotal",
    // Preference (Format Number Implementation)
    cell: ({ row }) => {
      const decimal = row.original.preferenceData?.data?.decimal || 2;

      return <p className="text-right">{formatNumber(row.original.totalSubtotal, decimal)}</p>;
    },
    filterFn: "includesString",
  },
  {
    id: "totalTaxAmount",
    accessorKey: "totalTaxAmount",
    header: "Total Tax Amount",
    cell: ({ row }) => {
      const decimal = row.original.preferenceData?.data?.decimal || 2;

      return <p className="text-right">{formatNumber(row.original.totalTaxAmount, decimal)}</p>;
    },
    filterFn: "includesString",
  },
  {
    id: "totalSubtotalTax",
    accessorKey: "totalSubtotalTax",
    header: "Total Subtotal (Tax)",
    cell: ({ row }) => {
      const decimal = row.original.preferenceData?.data?.decimal || 2;

      return <p className="text-right">{formatNumber(row.original.totalSubtotalTax, decimal)}</p>;
    },
    filterFn: "includesString",
  },
  {
    id: "roundingAmount",
    accessorKey: "roundingAmount",
    header: "Rounding Amount",
    cell: ({ row }) => {
      const decimal = row.original.preferenceData?.data?.decimal || 2;

      return <p className="text-right">{formatNumber(row.original.roundingAmount, decimal)}</p>;
    },
    filterFn: "includesString",
  },
  {
    id: "localNetTotal",
    accessorKey: "localNetTotal",
    header: "Local Net Total",
    cell: ({ row }) => {
      const decimal = row.original.preferenceData?.data?.decimal || 2;

      return <p className="text-right">{formatNumber(row.original.localNetTotal, decimal)}</p>;
    },
    filterFn: "includesString",
  },
  {
    id: "netTotal",
    accessorKey: "netTotal",
    header: "Net Total",
    cell: ({ row }) => {
      const decimal = row.original.preferenceData?.data?.decimal || 2;

      return <p className="text-right">{formatNumber(row.original.netTotal, decimal)}</p>;
    },
    filterFn: "includesString",
  },
  {
    id: "currencyCode",
    accessorKey: "currencyCode",
    header: "Currency Code",
    filterFn: "includesString",
  },
  {
    id: "currencyRate",
    accessorKey: "currencyRate",
    header: "Currency Rate",
    cell: ({ row }) => {
      const decimal = row.original.preferenceData?.data?.decimal || 2;
      
      return <p className="text-right">{formatNumber(row.original.currencyRate, decimal)}</p>;
    },
    filterFn: "includesString",
  },
  {
    id: "projectCode",
    accessorKey: "projectCode",
    header: "Project",
    filterFn: "includesString",
  },
  {
    id: "docValidity",
    accessorKey: "docValidity",
    header: "Doc. Validity",
    filterFn: "includesString",
  },
  {
    id: "shipperCode",
    accessorKey: "shipperCode",
    header: "Shipper",
    filterFn: "includesString",
  },
  {
    id: "taxExNo",
    accessorKey: "taxExNo",
    header: "Tax EX",
    filterFn: "includesString",
  },
  {
    id: "remark1",
    accessorKey: "remark1",
    header: "Remark 1",
    filterFn: "includesString",
  },
  {
    id: "remark2",
    accessorKey: "remark2",
    header: "Remark 2",
    filterFn: "includesString",
  },
  {
    id: "remark3",
    accessorKey: "remark3",
    header: "Remark 3",
    filterFn: "includesString",
  },
  {
    id: "remark4",
    accessorKey: "remark4",
    header: "Remark 4",
    filterFn: "includesString",
  },
  {
    id: "salesOrderNo",
    accessorKey: "salesOrderNo",
    header: "SO No.",
    filterFn: "includesString",
  },
  {
    id: "jobOrderNo",
    accessorKey: "jobOrderNo",
    header: "JO No.",
    filterFn: "includesString",
  },
  {
    id: "deliveryOrderNo",
    accessorKey: "deliveryOrderNo",
    header: "DO No.",
    filterFn: "includesString",
  },
  {
    id: "salesInvoiceNo",
    accessorKey: "salesInvoiceNo",
    header: "INV No.",
    filterFn: "includesString",
  },
  {
    id: "cashSalesNo",
    accessorKey: "cashSalesNo",
    header: "CS No.",
    filterFn: "includesString",
  },
  {
    id: "salesDebitNoteNo",
    accessorKey: "salesDebitNoteNo",
    header: "DN No.",
    filterFn: "includesString",
  },
   {
    id: "customerPaymentNo",
    accessorKey: "customerPaymentNo",
    header: "OR No.",
    filterFn: "includesString",
  },
  {
    id: "deliveryTerm",
    accessorKey: "deliveryTerm",
    header: "Delivery Terms",
    filterFn: "includesString",
  },
  {
    id: "deliveryAddress",
    accessorKey: "deliveryAddress",
    header: "Delivery Address",
    filterFn: "includesString",
  },
  {
    id: "note",
    accessorKey: "note",
    header: "Note",
    cell: ({ row }) => <DescriptionCellTooltip row={row} columnName="note" />,
    filterFn: "includesString",
  },
  {
    id: "attachment",
    accessorKey: "attachment",
    header: "Attachment",
    cell: ({ row }) => {
      const attachment = row.original.attachment;
      const attachmentLinks = row.original.attachmentLink || [];
      const attachmentFiles = row.original.attachmentFiles || [];

      // Handle new records with attachmentFiles
      if (attachmentFiles.length > 0) {
        return attachmentFiles
          .map((file, index) => {
            const link = attachmentLinks?.find((al) => al.name === file.name);
            return link ? (
              <a
                key={index}
                href={link.link}
                target="_blank"
                rel="noopener noreferrer"
                style={{ color: "blue" }}
              >
                {file.name}
              </a>
            ) : (
              <span key={index} style={{ color: "blue" }}>
                {file.name}
              </span>
            );
          })
          .reduce(
            (prev, curr, index) => (index === 0 ? [curr] : [prev, ", ", curr]),
            []
          );
      }

      // Handle existing records with attachment JSON
      if (!attachment) {
        return null;
      }

      try {
        const attachments = JSON.parse(attachment);

        if (!attachments || attachments.length === 0) {
          return null;
        }

        return attachments
          .map((att, index) => {
            const link = attachmentLinks.find((al) => al.name === att.Name);
            return link ? (
              <a
                key={index}
                href={link.link}
                target="_blank"
                rel="noopener noreferrer"
                style={{ color: "blue" }}
              >
                {att.Name}
              </a>
            ) : (
              <span key={index} style={{ color: "gray" }}>
                {att.Name}
              </span>
            );
          })
          .reduce((prev, curr) => [prev, ", ", curr]);
      } catch (error) {
        return null;
      }
    },
    filterFn: filterWithStringConversion,
  },
  {
    id: "description",
    accessorKey: "description",
    header: "Description",
    cell: ({ row }) => (
      <DescriptionCellTooltip row={row} columnName="description" />
    ),
    filterFn: "includesString",
  },
  {
    id: "approvalStatus",
    accessorKey: "approvalStatus",
    header: "Approval Status",
    filterFn: "includesString",
  },
  {
    id: "rejectReason",
    accessorKey: "rejectReason",
    header: "Reject Reason",
    filterFn: "includesString",
  },
  {
    id: "createdAtFormat",
    accessorKey: "createdAtFormat",
    header: "Created at",
    filterFn: "includesString",
  },
  {
    id: "CreatedByUsername",
    accessorKey: "CreatedByUsername",
    header: "Created by",
    filterFn: "includesString",
  },
  {
    id: "updatedAtFormat",
    accessorKey: "updatedAtFormat",
    header: "Updated at",
    filterFn: "includesString",
  },
  {
    id: "UpdatedByUsername",
    accessorKey: "UpdatedByUsername",
    header: "Updated by",
    filterFn: "includesString",
  },
  {
    id: "valid",
    accessorKey: "valid",
    header: "Validity",
    cell: ({ row }) => {
      const validity = row.getValue("valid") as Quotation["valid"];
      return validity === "1" ? "Valid" : "Invalid";
    },
    filterFn: "includesString",
  },
];

export const columns: ColumnDef<Quotation>[] = rawColumns.map(
  (col) => {
    if (typeof col.header === "string") {
      const sizes = autoWordSize(col.header);

      return {
        ...col,
        sizes,
        size: sizes, //applied automatically
        minSize:50
      };
    }

    return col;
  }
)