import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Opening/Closing Balance",
};

export default function OpeningClosingBalanceLayout({ children } : { children: React.ReactNode }) {
  return children;
}