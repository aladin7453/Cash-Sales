import type { Metadata } from "next";

export const metadata: Metadata = {
  //title: "Quotation",
};

export default function QuotationPageLayout({ children }: { children: React.ReactNode }) {
  return children;
}
