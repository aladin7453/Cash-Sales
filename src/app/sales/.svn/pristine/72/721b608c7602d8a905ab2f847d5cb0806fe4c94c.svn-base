import type { Metadata } from "next";
export const metadata: Metadata = {

  title: "Active Customer Report",
};
export default function ActiveCustomerReportPageLayout({ children }: { children: React.ReactNode }) {
  return children;
}
