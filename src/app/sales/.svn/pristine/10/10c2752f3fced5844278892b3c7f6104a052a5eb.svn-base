import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Customer Tag",
};

export default function CustomerTagPageLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
