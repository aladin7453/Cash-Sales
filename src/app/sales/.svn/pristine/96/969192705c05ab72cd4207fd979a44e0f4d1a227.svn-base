import type { Metadata } from "next";

export const metadata: Metadata = {
  //title: "Sales Invoice",
};

export default function SalesInvoicePageLayout({ children }: { children: React.ReactNode }) {
  return children;
}
