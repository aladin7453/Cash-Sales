import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Customer Listing",
};

export default function CustomerListingPageLayout({ children }: { children: React.ReactNode }) {
  return children;
}